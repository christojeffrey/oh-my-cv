// Style utilities and configuration
// This module is reserved for future style-related utilities
// Style atom exports are now in data.ts to avoid conflicts

// ResumeStyles interface is imported from data.ts to avoid duplicate exports
export type { ResumeStyles } from "./data";
